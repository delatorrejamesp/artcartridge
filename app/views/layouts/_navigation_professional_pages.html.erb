<nav class="navbar navbar-findcond navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%if user_signed_in?%>
                <%if current_page?(professional_page_url(subdomain: "#{@user.slug}")) || current_page?(professional_page_portfolio_url(subdomain: "#{@user.slug}")) || current_page?(professional_page_contact_url(subdomain: "#{@user.slug}")) ||
                current_page?(professional_page_shop_url(subdomain: "#{@user.slug}")) || current_page?(professional_page_about_url(subdomain: "#{@user.slug}")) %>
                <%=link_to "#{@user.full_name}", professional_page_url(@user, subdomain: "#{@user.slug}"), class: "navbar-brand"%>
                <%else%>
                <%#if Rails.env.production?%>
                  <%# url = "http://www.artcartridge.com"%>
                <%#else%>
                  <%# url = authenticated_root_url(subdomain: false)%>
                <%#end%>
                <% url = "http://www.artcartridge.com"%>
                <% url = "http://#{url}" unless url=~/^https?:\/\//%>
                <%=link_to "#{url}", class: "navbar-brand" do %>
                <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
            <%end%>
            <%=link_to "@#{@user.username}", community_page_url(subdomain: "#{@user.slug}"), class: "navbar-brand"%>
            <%end%>
            <%else%>
                <%=link_to  unauthenticated_root_url, class: "navbar-brand" do %>
                <%=@user.full_name%>
            <%end%>
            <%end%>
        </div>
        <div class="collapse navbar-collapse" id="navbar">
            <ul class="nav navbar-nav navbar-right">
                <%if current_page?(professional_page_url(subdomain: "#{@user.slug}")) || current_page?(professional_page_portfolio_url(subdomain: "#{@user.slug}")) || current_page?(professional_page_contact_url(subdomain: "#{@user.slug}")) ||
                current_page?(professional_page_shop_url(subdomain: "#{@user.slug}")) || current_page?(professional_page_about_url(subdomain: "#{@user.slug}")) %>
                <li class="active"><%=link_to "Portfolio", professional_page_portfolio_url(subdomain: "#{@user.slug}")%></li>
                <li class="active"><%=link_to "About", professional_page_about_url(subdomain: "#{@user.slug}")%></li>
                <li class="active"><%=link_to "Contact", professional_page_contact_url(subdomain: "#{@user.slug}")%></li>
                <li class="active"><%=link_to "Shop", professional_page_shop_url(subdomain: "#{@user.slug}")%></li>
                <li class="active"><%=link_to "Profile", community_page_url(subdomain: "#{@user.slug}")%></li>
                <%end%>
                  <%if current_page?(community_page_url(subdomain: "#{@user.slug}"))%>
                  <%if user_signed_in?%>
                    <li class="active"><%=link_to "Upload", new_photo_path, remote: true, data: { target: "uploaded" }%></li>
                  <%end%>
                  <li style="padding: 0px!important;">
                      <%if user_signed_in?%>
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" style="padding: 10px 0px 10px 0px;">
                            <%=image_tag current_user.avatar.url(:thumb), class: "img-circle", size: "30x30"%>
                        </a>
                        <ul class="dropdown-menu" role="menu">
                            <%=render partial: "layouts/navigation_subdomain_links"%>
                        </ul>
                        <%end%>
                  </li>
                <%else%>
                  <%if user_signed_in?%>
                    <li style="padding: 0px!important;">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false" style="padding: 12px 0px 10px 0px;">
                            <span class="glyphicon glyphicon-cog" style="font-size:22px;"></span>
                        </a>
                        <ul class="dropdown-menu" role="menu">
                            <%=render partial: "layouts/navigation_subdomain_links"%>
                        </ul>
                    </li>
                  <%end%>
                <%end%>
            </ul>
        </div>
    </div>
</nav>
