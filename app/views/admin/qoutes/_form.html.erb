<%= form_for(admin_qoute) do |f| %>
  <% if admin_qoute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_qoute.errors.count, "error") %> prohibited this admin_qoute from being saved:</h2>

      <ul>
      <% admin_qoute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :enable %>
    <%= f.check_box :enable %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
